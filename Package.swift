// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "SwiftOnSockets",
    products: [
        .library(
            name: "SwiftOnSockets",
            targets: ["SwiftOnSockets"])
    ],
    targets: [
        .target(
            name: "SwiftOnSockets",
            dependencies: [])
    ]
)
